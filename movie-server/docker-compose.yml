version: '3.8'

services:
  dev-db:
    image: postgres
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: MovieNest
    networks:
      - MovieNest_network

networks:
  MovieNest_network:
    driver: bridge
